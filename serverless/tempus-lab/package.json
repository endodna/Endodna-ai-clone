{
  "name": "tempus-lab-lambda",
  "version": "1.0.0",
  "description": "Lambda function for Tempus Lab upload notification via API Gateway",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepare": "npm run clean && npm run build"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.450.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.9.0",
    "typescript": "^5.2.2"
  }
}

